# Expected SHA-256 digests of the published worktree-gate release assets
# (v0.24.0). This file is the trust root (D5): the network supplies only
# bytes, the repository supplies the expected digest. A downloaded asset
# whose SHA-256 does not match is deleted and never executed.
#
# Reproduce any digest locally with:
#   scripts/build-gate.sh
#   shasum -a 256 dist/worktree-gate-*
#
# CANONICAL TOOLCHAIN: Go builds are deterministic for a given toolchain, but
# different Go releases compile different stdlib bytes. These digests were
# generated with go1.24.13 — the same version the release CI pins
# (.github/workflows/release-worktree-gate.yml) — and MUST be regenerated with
# exactly that version, or the CI checksum gate fails the release.
#
# The four executable assets are intentionally CI/release outputs, not tracked
# files. These lines are their source-level trust manifest; regenerate them with
# scripts/build-gate.sh after any Go gate change and verify before publishing.
#
# Regenerated as part of the release (task 4.9): digests MUST match the
# published assets; the CI release workflow diffs its generated SHA256SUMS
# against this file and fails the release on any mismatch.
#
# Regenerated for the go-binding-witness strangler slice (the same four-arch asset
# gains the Go-owned `--resolve-bindings` capability binding/conflict grading and
# the Go-written durable tracker witness; no new asset, no second trust root), with
# the canonical go1.24.13 toolchain via scripts/build-gate.sh && (cd dist && shasum
# -a 256 worktree-gate-* | grep -v current).
#
# Regenerated after trello-ledger-integration (the same four-arch asset gains the
# `--write open|link|close|exempt` verb and the bounded store lock; no new asset, no
# second trust root) and again after ledger-remote-reconciliation (the same
# four-arch asset gains Go-owned remote-observation reconciliation; no new asset,
# no second trust root) with the canonical go1.24.13 toolchain via:
#   scripts/build-gate.sh && (cd dist && shasum -a 256 worktree-gate-* | grep -v current)
#
# Regenerated again after the tracker-ledger follow-ups (the same four-arch asset gains
# the optional `config_field_when_set` reconcile mapping and the corroborated closed-item
# binding; no new asset, no second trust root), same canonical go1.24.13 toolchain and the
# same command above.
#
# Regenerated for the Worktree Ledger port and release v0.24.0 (the same four-arch
# asset gains the provider-neutral Worktree domain port, current-tip-bound PR evidence,
# and deterministic cleanup coverage; no new asset, no second trust root), with the
# canonical go1.24.13 toolchain and the same command above.
#
# Regenerated for the go-orphan-cleanup strangler slice (the same four-arch asset
# gains the Go-owned `--plan-orphans` materialization orphan planning; no new
# asset, no second trust root), with the canonical go1.24.13 toolchain via:
#   scripts/build-gate.sh && (cd dist && shasum -a 256 worktree-gate-* | grep -v current)
# Regenerated for the go-resolved-config strangler slice (the same four-arch
# asset gains the Go-owned `--plan-resolved-config` manifest projection query;
# no new asset, no second trust root), with the canonical go1.24.13 toolchain
# via:
#   scripts/build-gate.sh && (cd dist && shasum -a 256 worktree-gate-* | grep -v current)
#
# Regenerated for the go-classify-override strangler slice (the same four-arch
# asset gains the Go-owned `--plan-classify` managed-override classification;
# no new asset, no second trust root), with the canonical go1.24.13 toolchain
# via:
#   scripts/build-gate.sh && (cd dist && shasum -a 256 worktree-gate-* | grep -v current)
# Regenerated for the materialization-sync-tag-conflicts strangler slice (the same
# four-arch asset gains the Go-owned `--resolve-tag-conflicts` advisory recipe
# tag-conflict grading; no new asset, no second trust root), with the canonical
# go1.24.13 toolchain via:
#   scripts/build-gate.sh && (cd dist && shasum -a 256 worktree-gate-* | grep -v current)
# Regenerated for the go-merge-config strangler slice (the same four-arch asset
# gains the Go-owned `--plan-merge-config` recipe merge_config decision; no new
# asset, no second trust root), with the canonical go1.24.13 toolchain via:
#   scripts/build-gate.sh && (cd dist && shasum -a 256 worktree-gate-* | grep -v current)
# Regenerated for the merge-config CPython repr parity fix (the same four-arch
# asset's pyRepr now matches CPython repr() quote selection and control-char
# escapes; no new asset, no second trust root), with the canonical go1.24.13
# toolchain via the same command above.
#
# Regenerated for the go-primitive-conflicts strangler slice (the same four-arch
# asset gains the Go-owned `--resolve-primitive-conflicts` recipe
# skill/command/mcp conflict grading; no new asset, no second trust root), with
# the canonical go1.24.13 toolchain via:
#   scripts/build-gate.sh && (cd dist && shasum -a 256 worktree-gate-* | grep -v current)
#
# Regenerated for the go-reconcile-stamps strangler slice (the same four-arch
# asset gains the Go-owned `--plan-reconcile-stamps` per-recipe reconcile stamp
# planning; no new asset, no second trust root), with the canonical go1.24.13
# toolchain via:
#   scripts/build-gate.sh && (cd dist && shasum -a 256 worktree-gate-* | grep -v current)
#
# Regenerated for the go-apply-orphans strangler slice (the same four-arch asset
# gains the Go-owned `--apply-orphans` orphan cache-directory deletion actuator;
# no new asset, no second trust root), with the canonical go1.24.13 toolchain
# via the same command above.
#
# Regenerated for the go-toml-writer strangler slice (the same four-arch asset
# gains the Go-owned `--write-recipe-config` recipe-config TOML writer core;
# no new asset, no second trust root), with the canonical go1.24.13 toolchain
# via the same command above.
#
# Regenerated for the go-lock-copy strangler slice (the same four-arch asset
# gains the Go-owned `--write-lock` lock-file writer core, byte-exact with
# lib/_internal/lock.py write_lock; no new asset, no second trust root), with
# the canonical go1.24.13 toolchain via the same command above.
#
034f62e1729b8d3556f974bf99057f78f467c2d60c4477c30f5e34896fd92718  worktree-gate-darwin-amd64
c2eb687e9671d541db50b5198de646b914621b73ecf4b6993648cd203ff5d91c  worktree-gate-darwin-arm64
6a6d0ac831c6d9ca0006714d8d7f9897a5a8300ef24f8722acce876daf5c8873  worktree-gate-linux-amd64
4470e6fb57302124148f4b8ecb37e5c92d4e778777a88ffd8d02bbbe1d101301  worktree-gate-linux-arm64
