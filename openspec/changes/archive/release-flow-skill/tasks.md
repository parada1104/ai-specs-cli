# release-flow-skill

Depth: light

## Goal

Add a local `release-flow` skill that documents this repo's dogfooded release
ritual, aligned with protected-head policy (`release/v*` → `main`).

## Tasks

- [x] Create `ai-specs/skills/release-flow/SKILL.md` with frontmatter + playbook
- [x] Validate skill metadata via skill-sync

## Out of scope

- Catalog recipe / new capability
- Changes to `git-merge-workflow` (already prefers `release/v*`)
