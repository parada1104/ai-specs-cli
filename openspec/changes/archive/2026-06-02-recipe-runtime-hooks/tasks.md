# Tasks: Recipe-declared runtime hooks, distributed to every harness

Runner: `./tests/validate.sh` (py_compile + bash -n + unittest). Strict TDD.

## Batch 1: RED — schema validation tests

- [x] 1.1 [TEST-RED] `tests/test_recipe_schema.py` (or existing schema test): add `test_provides_hooks_valid` — recipe with a valid `[[provides.hooks]]` (id/event/script) parses and registers the hook; FAILS (schema doesn't know `hooks`).
- [x] 1.2 [TEST-RED] add `test_provides_hooks_missing_field` (omit id/event/script → explicit error), `test_provides_hooks_unknown_event` (bad event → error listing known events), `test_provides_hooks_script_escape` (`../`/absolute → path-escape error), `test_provides_hooks_absent_ok` (no hooks → passes, none registered). All FAIL.
- [x] 1.3 [TEST-GREEN gate] Run `./tests/run.sh`; confirm only the new schema tests are RED, all prior GREEN. Record in apply-progress.

## Batch 2: Schema + materialization (GREEN for Batch 1)

- [x] 2.1 [IMPL] `lib/_internal/recipe-materialize.py`: parse `[[provides.hooks]]` into a Hook dataclass; validate required fields, known abstract event set (`pre-tool-use`, `post-tool-use`, `session-start`, `stop`), and script-path-inside-recipe (reuse the init-prompt path-escape guard).
- [x] 2.2 [IMPL] Materialize each hook script to `ai-specs/recipes/<recipe-id>/hooks/<script>` and `chmod +x`. Mirror existing template materialization.
- [x] 2.3 [IMPL] Add `--resolved-hooks-out <path>`: emit `{enabled_agents, hooks:[{recipe,id,event,matcher,blocking,script_path,env}]}`; `env` carries resolved `[config.*]` values the hook needs. Mirror `--resolved-config-out`.
- [x] 2.4 [TEST-GREEN] Batch 1 tests pass; add a materialization test (script lands at the neutral path, executable). Run `./tests/run.sh`.

## Batch 3: RED — render tests

- [x] 3.1 [TEST-RED] `tests/test_hooks_render.py` (new): `test_claude_pretooluse_managed_block` — fixture resolved-hooks → `.claude/settings.json` contains the managed-block `PreToolUse` entry with matcher + command → script_path. FAILS (no renderer).
- [x] 3.2 [TEST-RED] `test_cursor_hooks_json`, `test_opencode_plugin_shim`, `test_pi_extension_shim` — each asserts the native wiring/shim content for that harness.
- [x] 3.3 [TEST-RED] `test_unsupported_event_warns_and_skips` — an event unmapped for a harness → warning emitted (recipe/hook/event/harness), hook absent for that harness, present for others.
- [x] 3.4 [TEST-RED] `test_render_idempotent` (second render byte-identical) and `test_user_hook_preserved` (user entry outside managed block survives). All FAIL.
- [x] 3.5 [TEST-GREEN gate] Confirm only new render tests RED.

## Batch 4: Renderer (GREEN for Batch 3)

- [x] 4.1 [IMPL] `lib/_internal/hooks-render.py` (new): load `--resolved-hooks`; define the abstract→native EVENT_MAP (design table) as the single source of truth.
- [x] 4.2 [IMPL] Claude (direct): merge managed block into `.claude/settings.json` (`PreToolUse` etc., matcher, command → script_path, `env`). Preserve sibling user keys.
- [x] 4.3 [IMPL] Adapters for non-native harnesses:
  - Cursor: generate `.cursor/hooks/<recipe>-<hook>.sh` wrapper (exec script, `exit 2` → `{"permission":"deny",...}` snake_case, else allow) + reference it from `.cursor/hooks.json` under the mapped native event; warn-and-skip when matcher targets file writes (no pre-file-write hook).
  - OpenCode: generate `.opencode/plugin/<recipe>-<hook>.ts` (`tool.execute.before` → `spawn` script, stdin normalized event, `exit 2` → `throw`). Header `// GENERATED by ai-specs`. Document subagent/MCP bypass (#5894/#2319).
  - Pi: generate `.pi/extensions/<recipe>-<hook>.ts` importing `@earendil-works/pi-coding-agent` (`pi.on("tool_call",…)` → `spawn` script, `exit 2` → `return {block:true,reason}`).
- [x] 4.4 [IMPL] Warn-and-skip for unmapped (event, harness); never emit broken wiring.
- [x] 4.5 [IMPL] Managed-block keying (`ai-specs:hooks:<recipe>:<hook>`) + idempotent rewrite.
- [x] 4.6 [TEST-GREEN] Batch 3 tests pass. Run `./tests/run.sh`.

## Batch 5: Pipeline wiring

- [x] 5.1 [IMPL] `lib/_internal/platform.sh`: per-agent hook target descriptors (settings.json path+key, hooks.json path, opencode plugin dir, pi extensions dir).
- [x] 5.2 [IMPL] `lib/sync.sh`: `mktemp RESOLVED_HOOKS_TEMP`; pass `--resolved-hooks-out` to materialize, `--resolved-hooks` to `hooks-render.py`; `rm -f` at cleanup.
- [x] 5.3 [IMPL] `lib/sync-agent.sh`: invoke `hooks-render.py` in the per-agent fan-out (enabled agent + resolved-hooks); add to the derived-artifacts list.
- [x] 5.4 [TEST] `tests/test_sync_pipeline.py`: end-to-end — manifest enabling a recipe with a hook, run `sync`, assert each enabled harness got its wiring; second run byte-identical.

## Batch 6: First consumer + docs

- [x] 6.1 [IMPL] `catalog/recipes/worktree-flow/recipe.toml`: declare the worktree-gate hook via `[[provides.hooks]]`; drop the prototype `[[provides.templates]]` hook entry; bump version once.
- [x] 6.2 [IMPL] Align `worktree-gate.sh` to the normalized event contract; reuse the 6 prototype test cases as an integration test.
- [x] 6.3 [IMPL] Update the dogfood manifest pin for worktree-flow to the new version; update any fixture pinning the old version.
- [x] 6.4 [DOCS] `docs/recipe-schema.md` (hooks primitive), `docs/runtime-hooks.md` (distribution + event map + status), `README.md` compatibility table (flip "runtime hooks: fanned out" once shipped).
- [x] 6.5 [VERIFY] `./tests/validate.sh` green; manual `ai-specs sync` on a fixture project shows all four harness wirings; `ai-specs sync` twice → no diff.

## Open-question gates — RESOLVED by research (June 2026); pin during apply

- [x] OQ1 Cursor events confirmed (10-event set); generic `preToolUse`/`postToolUse`/`subagent*` UNCONFIRMED → warn-and-skip. Decision via stdout `permission` JSON (snake_case), not exit code → Cursor needs the shell wrapper. No pre-file-write hook.
- [x] OQ2 OpenCode globs both `plugin/`+`plugins/`; emit `plugin/`. Block = `throw`. Gaps: subagent #5894, MCP #2319.
- [x] OQ3 Pi = `@earendil-works/pi-coding-agent` (old `@mariozechner` deprecated), Node ≥22.19, jiti, CI-safe. Block = `return {block:true}`.
- [x] OQ4 Pin each adapter to the user's installed harness version (events are version-sensitive); choose the round-tripping Claude settings.json managed-block shape.
