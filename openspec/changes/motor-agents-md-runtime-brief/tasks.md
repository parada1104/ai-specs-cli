# Tasks: `motor-agents-md-runtime-brief`

Split `AGENTS.md` into a concise runtime brief (generated from `ai-specs.toml`) and a separate skill/capability registry artifact (`ai-specs/.skill-registry.md`).

---

## 1. Core Renderer Rewrite

- [x] 1.1 Rewrite `lib/_internal/agents-md-render.py` to parse `ai-specs.toml` and emit a runtime brief instead of a skill registry
- [x] 1.2 Implement runtime brief sections: Project identity, Enabled runtimes, MCPs (redacted), Active recipes/bindings/capabilities, Safety rules, Context sources, Conflict policy, Workflow rules, Useful commands
- [x] 1.3 Implement MCP secrets redaction: show env variable references (`$VAR`, `${VAR}`, `{env:VAR}`); replace all other literal values in `env`/`headers` with `***`
- [x] 1.4 Add runtime-brief marker detection (`<!-- ai-specs:runtime-brief -->`): if present in existing `AGENTS.md`, skip overwriting entirely and print an informative message
- [x] 1.5 Add `<!-- ai-specs:generated-runtime-brief -->` marker to auto-generated output so tests can distinguish auto-generated from manual files
- [x] 1.6 Remove skills index table, Auto-invoke placeholder, and legacy footer from generated `AGENTS.md`; keep only concise runtime operational context
- [x] 1.7 Ensure deterministic output ordering (sorted MCPs, recipes, rules) and no timestamps or random identifiers for idempotency

## 2. Registry Artifact Generation

- [x] 2.1 Modify `ai-specs/skills/skill-sync/assets/sync.sh` to generate `ai-specs/.skill-registry.md` instead of inserting Auto-invoke sections into `AGENTS.md`
- [x] 2.2 Implement registry artifact header with `> **Auto-generated by ai-specs sync. Do not edit by hand.**`
- [x] 2.3 Implement Skill Index table (columns: Skill, Source, Description, Path) covering local, recipe, and dependency skills with deterministic sorting
- [x] 2.4 Implement Auto-invoke Mappings table (columns: Action, Skill, Scope) with one row per trigger per scope per skill, sorted deterministically
- [x] 2.5 Ensure `sync.sh` skips `AGENTS.md` entirely (not just Auto-invoke insertion) when the runtime-brief marker is present
- [x] 2.6 Preserve monorepo scope mapping logic in `sync.sh`; ensure registry artifact generation works for both standalone and monorepo layouts
- [x] 2.7 Ensure registry artifact path is `ai-specs/.skill-registry.md` and that it is regenerated (overwriting hand-edits) on every sync

## 3. Test Updates and New Tests

- [x] 3.1 Update `test_sync_pipeline.py` assertions that check for skills table / Auto-invoke rows in `AGENTS.md` to instead check `ai-specs/.skill-registry.md`
- [x] 3.2 Update `test_sync_pipeline.py` assertions for `AGENTS.md` content to match the new runtime brief format (project identity, no skills catalog)
- [x] 3.3 Update `SkillSyncScriptTests` to verify registry artifact generation instead of `AGENTS.md` modification
- [x] 3.4 Add idempotency test: running `ai-specs sync` twice produces byte-identical `AGENTS.md`
- [x] 3.5 Add idempotency test: running `ai-specs sync` twice produces byte-identical `ai-specs/.skill-registry.md`
- [x] 3.6 Add test for runtime-brief marker preservation: existing `AGENTS.md` with `<!-- ai-specs:runtime-brief -->` is not overwritten by sync
- [x] 3.7 Add test for MCP secrets redaction: literal secrets in `ai-specs.toml` appear as `***` in generated `AGENTS.md`, env references are preserved
- [x] 3.8 Add test for registry artifact completeness: local, recipe, and dependency skills all appear in the Skill Index with correct Source values
- [x] 3.9 Add test for manual-only skills: skills without `scope` or `auto_invoke` appear in Skill Index but not in Auto-invoke table
- [x] 3.10 Add test for incomplete sync metadata failure: skill with `scope` but no `auto_invoke` (or vice versa) fails sync with actionable error naming the skill

## 4. Documentation Updates

- [x] 4.1 Update README to describe the new `AGENTS.md` purpose (runtime brief) and the existence of `ai-specs/.skill-registry.md`
- [x] 4.2 Update README to explain that `ai-specs/.skill-registry.md` is auto-generated and must not be hand-edited
- [x] 4.3 Update `ai-specs/contracts/skill-frontmatter.md` (or relevant contract docs) to reference the registry artifact instead of `AGENTS.md` for Auto-invoke mappings
- [x] 4.4 Update inline comments in `agents-md-render.py` and `sync.sh` to reflect the new responsibilities
- [x] 4.5 Verify the existing `AGENTS.md` in this repo retains its manual runtime-brief content and marker after the changes

## 5. Validation and Integration

- [x] 5.1 Run `./tests/run.sh` and ensure all existing tests pass after updates
- [x] 5.2 Run `./tests/validate.sh` and ensure syntax validation passes for all changed files
- [x] 5.3 Perform a manual `ai-specs sync` in a temporary workspace to verify the generated runtime brief and registry artifact match the spec scenarios
- [x] 5.4 Review the generated `AGENTS.md` for this repo to confirm the manual runtime brief is preserved and the registry artifact is created correctly
- [x] 5.5 Check that no unintended modifications occur in `.recipe/`, `.deps/`, or `ai-specs/` on re-sync (idempotency smoke test)
