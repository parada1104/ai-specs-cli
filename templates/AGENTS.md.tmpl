# {{PROJECT_NAME}} — Agent Instructions

Source of truth for AI agents (Claude, Cursor, OpenCode, Codex, Copilot, Gemini)
working on this project. Per-agent files (`CLAUDE.md`, `GEMINI.md`, etc.) are
symlinks to this file.

## Project

(Describe the project: purpose, stack, conventions.)

## How AI tooling is wired

This project uses the **`ai-specs`** standard, driven by the global
[`specs-ai`](https://github.com/nnodes/specs-ai-cli) CLI. The manifest at
`ai-specs/ai-specs.toml` declares enabled agents, vendored skills, and MCP
servers. Per-agent configs (`.claude/`, `.cursor/`, `opencode.json`, ...) are
**generated** from the manifest — do not hand-edit; they're gitignored.

Install the CLI once (global):

```bash
curl -fsSL https://raw.githubusercontent.com/nnodes/specs-ai-cli/main/install.sh | bash
```

Common commands (run from project root):

```bash
specs-ai sync                      # vendor deps + regenerate AGENTS.md + fan-out per agent
specs-ai add-skill <name>          # local skill (lives in this repo)
specs-ai add-dep <git-url>         # vendored skill (external, gitignored)
specs-ai sync-agent --claude       # re-render configs for a single agent
```

## Skills

All skills live in [`ai-specs/skills/`](ai-specs/skills/). Load with
`Read ai-specs/skills/<name>/SKILL.md`.

> Bootstrap entries (replaced by `skill-sync`):
>
> [SKILL.md](ai-specs/skills/skill-creator/SKILL.md)
> [SKILL.md](ai-specs/skills/skill-sync/SKILL.md)

### Auto-invoke Skills

(Regenerated by `skill-sync`. Do not edit by hand.)

When performing these actions, ALWAYS invoke the corresponding skill FIRST:

| Action | Skill |
|--------|-------|
