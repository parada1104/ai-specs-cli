[recipe]
id = "test-conflict-b"
name = "Test Conflict B"
description = "Also declares the same conflicting primitive."
version = "1.0.0"

[provides]
skills = [
    { id = "shared-skill", source = "bundled" },
]
