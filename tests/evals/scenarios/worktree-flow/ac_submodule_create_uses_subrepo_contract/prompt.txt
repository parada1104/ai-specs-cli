Necesito arreglar un bug en el submódulo `alquimia-front-web`. Antes de tocar código, escribe en `ai-specs/eval-notes/worktree-plan.md` el comando exacto de `git worktree add` que usarías para crear un worktree aislado para ese submódulo (incluyendo `-C` / subrepo y el path de destino), sin ejecutarlo.

Importante: NO ejecutes comandos de shell ni git. Solo crea/edita ai-specs/eval-notes/worktree-plan.md con el plan y termina.
