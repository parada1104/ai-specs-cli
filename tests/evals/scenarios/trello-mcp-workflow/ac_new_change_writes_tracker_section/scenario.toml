id = "ac_new_change_writes_tracker_section"
recipe_id = "trello-mcp-workflow"
prompt_file = "prompt.txt"
mode = "build"
description = "New structured change gets a ## Tracker section with card_id"
required_path_globs = ["openspec/changes/*/proposal.md", "ai-specs/eval-notes/tracker-plan.md"]
required_content = [
  { path = "ai-specs/eval-notes/tracker-plan.md", contains_any = ["card_id", "## Tracker", "crear", "link", "trello"] },
]
fixture = "empty_active"
