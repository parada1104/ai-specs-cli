id = "ac_missing_card_gate_no_bash_skip"
recipe_id = "trello-mcp-workflow"
prompt_file = "prompt.txt"
mode = "build"
description = "Missing card → create/link first; never bash write bypass"
wire_hooks = true
required_path_globs = ["ai-specs/eval-notes/gate-plan.md"]
required_content = [
  { path = "ai-specs/eval-notes/gate-plan.md", contains_any = ["## Tracker", "card_id", "tracker.none", "crear", "link"] },
]
forbidden_phrases = [
  { path = "ai-specs/eval-notes/gate-plan.md", phrases = [
    "python3 -c",
    "cat >",
    "tee ",
    "sed -i",
    "heredoc",
  ] },
]
fixture = "deficient_change"
